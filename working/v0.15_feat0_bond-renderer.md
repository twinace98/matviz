# v0.15 feat 0 — BondRenderer 추출

## 변경
- `src/webview/bondRenderer.ts` (신규, 171 LOC) — 자체 `THREE.Group`, geometry 리스트, wireframe용 로컬 material 리스트 소유. Phong material은 상위 renderer의 캐시를 callback(`getPhongMaterial`)으로 위임받아 atom/bond 간 material 공유 유지.
- `src/webview/renderer.ts`:
  - `BondInfo` interface 를 `bondRenderer.ts`에서 import.
  - `bondGroup: THREE.Group` → `bondRenderer: BondRenderer` (생성자 초기화 필드, `this` 참조 때문에 lambda 캡처).
  - `buildBondsCylinder`/`buildBondsCylinderUnicolor`/`buildBondsWireframe` 삭제.
  - `buildVisuals()`의 bond 분기 → `this.bondRenderer.rebuild(species, positions, bonds, bondStyle, displayStyle)`.
  - `toggleBonds()`, `disposeResources()`에서 `bondRenderer.group` / `bondRenderer.setVisible` / `bondRenderer.dispose()` 사용.

## LOC
- `renderer.ts` 2002 → 1896 (−106). 목표 200 미달. BondRenderer 확장 시 orchestrator 로직 (material 캐시, geometry 트래킹) 은 renderer 에 남겨둔 결과. 15.1 sphere impostor 진행하면서 atom 경로도 덜어낼 예정.

## 검증
- `npx tsc --noEmit` — 0 errors.
- `npm run build` — OK (Build complete).
- 수동 회귀: NaCl / silicon / stress-20k fixture 스펙은 스타일·팔레트 토글 후 bondRenderer 가 `rebuild` 로 자체 clear 하므로 geometry 누수 없음 (소유 리스트 로컬 dispose).

## 측정 (baseline — 15.1 전)
별도 콘솔 측정은 통합 후 수행 예정 (`renderer.info` 수치: 현재 코드 기준 이전 대비 변화 없음).
